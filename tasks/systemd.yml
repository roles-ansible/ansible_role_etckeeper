---
- name: Make sure etckeeper is started and enabled
  become: true
  ansible.builtin.systemd_service:
    name: 'etckeeper.service'
    enabled: true

- name: Make sure etckeeper timer is enabled
  become: true
  ansible.builtin.systemd_service:
    name: 'etckeeper.timer'
    enabled: true
